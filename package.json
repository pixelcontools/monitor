{
  "name": "pixelcon-monitoring",
  "version": "1.0.0",
  "description": "Static client-side GeoPixels monitor for GitHub Pages",
  "scripts": {
    "build": "node build.js",
    "dev": "npx http-server . -p 3000 -o"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
